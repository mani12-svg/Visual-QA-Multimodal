# Visual-QA-Multimodal
#An AI model that analyzes uploaded images and directly answers user questions about the visual content.
#Tech Stack: Python, LangChain, Multimodal LLM
#Installation/Usage: 1. Clone the repository. 2. Install dependencies via pip install -r requirements.txt. 3. Open the Colab #notebook and run all cells.
